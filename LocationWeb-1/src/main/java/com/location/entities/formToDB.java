package com.location.entities;

public class formToDB {
	/*public static void main(String[] args) {
		
		LocationServImpl ls = new LocationServImpl();
		List<WpUser> wpUsers = ls.getAllLocation();
		for(WpUser temp: wpUsers) {
			System.out.println(temp.getUser_email());
		}
	}*/
	
	/** DB application prop
	 * jdbc:mysql://localhost:3306/projectdb
	 */
	
	/*<?php
		    if (!empty($_POST)) {
		        global $wpdb;
		        $table = wp_achord;
		        $data = array(
		            'name' => $_POST['yourname'],
		            'chord'    => $_POST['chord']
		        );
		        $format = array(
		            '%s',
		            '%s'
		        );
		        $success=$wpdb->insert( $table, $data, $format );
		        if($success){
		            echo 'data has been save' ; 
		        }
		    } else {
		        ?>
		        <form method="post">
		            <input type="text" name="yourname">
		            <textarea name="chord"></textarea>
		            <input type="submit">
		        </form>
		        <?php 
		    }  
		?>*/
}
